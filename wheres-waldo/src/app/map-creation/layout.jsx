import "../globals.css";

export const metadata = {
  title: "Level Creation",
  description: "Upload and create wheres waldo levels",
};

export default function MapCreationLayout({ children }) {
  return <div className="page-layout">{children}</div>;
}
